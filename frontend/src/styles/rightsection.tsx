import React form 'react';
import openai from 'OpenAI';
import {
    UseState,AfterState,NewState
} from 'react-dom';

import React-dom from 'react-dom';
import React-swc from 'react-hot-toast';
import { ReactNode } from 'react';
import Reflect from 'react';
import App from '../App.tsx';


Function MyApp=()=>{
    return(
        <>
        document.getElementByClassName.(div1)=<root>
        render.singin
    </root>
    document.getElementByClassName.
    (div2)=<root>
            render.singup
        </root>
        document.getElementByClassName.(div3)=<root>
            render.main
        </root>
        document.getElementByClassName.(div4)=<root>
            render.leftSection
        </root>

        </>
    )

}

